export default function SharpPipe(filename: string, dir: string): Promise<{
    filename: string;
    path: string;
}>;
export declare function MakeWatermark(filename: string, dir: string): Promise<{
    filename: string;
    path: string;
}>;
